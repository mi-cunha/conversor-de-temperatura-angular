import { DataBrandPipe } from './data-brand.pipe';

describe('DataBrandPipe', () => {
  it('create an instance', () => {
    const pipe = new DataBrandPipe();
    expect(pipe).toBeTruthy();
  });
});
